class Solution {
public:
    bool isValid(string s) {
        vector<char> CharStack;
        for( int i = 0; i < s.size();i++ ) {
            
            if ( s[i] == '(' || s[i] == '[' || s[i] == '{' ){
                
                CharStack.push_back( s[i] );
            
            }
            else {
                if (  ){
                    
                }
                    
            }
            
        }
        if ( stackSmall == 0 && stackMid == 0 && stackBig == 0 )
            return true;
        return false;
    }
};
